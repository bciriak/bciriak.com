# Dokploy / Nixpacks build for an Astro static site.
# Build produces ./dist which Dokploy serves via its static handler.
# Astro 6 requires Node >=22.12.

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "echo 'static build complete'"

[staticAssets]
distDir = "dist"
